# UVIC Engineering Materials Database — Claude Context

## Project

Static website for UVIC Design Engineering students to browse, compare, and reference engineering material properties. Hosted on GitHub Pages. **For educational use only — not for safety-critical applications.**

## Stack

- **Frontend:** Plain HTML/CSS/JS, ES modules (`type="module"`), no build step
- **Charts:** Chart.js 4.x via CDN
- **Analytics:** GoatCounter (privacy-friendly, no cookies) — script tag on all 6 top-level pages, dashboard at `scottroberts8314.goatcounter.com`; GitHub's own traffic tab does not count visits to the Pages site itself
- **Data:** JSON files (`materials/`, `references/`)
- **Admin tooling:** Python scripts in `tools/`, venv at `.venv/`
- **Dev server:** `python -m http.server` from project root (ES modules require a server, not `file://`)

## File Structure

```
index.html          Browse/search page
material.html       Detail view (?slug=...)
compare.html        Comparison charts (?slugs=a,b,c)
select.html         Advanced Selection: pre-filters → property ranges → merit-index ranking
submit.html         Dual-mode: add new material OR edit existing (?slug=...)
404.html            Custom not-found page

css/base.css        Reset, CSS variables, site header
css/components.css  Badges, buttons, cards
css/pages/          Page-specific styles (incl. select.css)

js/core/
  loader.js         Fetch manifest + materials + references (cached)
  store.js          In-memory Map cache
  schema.js         migrateToLatest() — currently v1, empty migrations
  units.js          All unit conversions
  derived.js        Shear modulus, specific stiffness, M1–M11 merit indices
  tooltips.js       Hover tooltip definitions for all properties
  export.js         buildRows() + downloadCSV() + downloadXLSX() — used by detail.js and compare.js
  disclaimer.js     Educational-use modal shown once per session; loaded on every page
  htmlImport.js     Client-side port of tools/parse_refs.py — parses uploaded reference HTML
                    for submit.js's "Pre-fill from HTML" button. See "HTML import" below —
                    this duplicates tools/parse_refs.py's site-scraping rules deliberately.

js/pages/
  browse.js         Filter/search/card rendering
  detail.js         Property tables, unit pickers, reference badges
  compare.js        Bar charts, S-N overlay, CTE vs temp, merit index table
  select.js         Three-tier filter (categorical → numeric range → merit-index rank), results table
  submit.js         Submission form, JSON download, reference management, edit mode

materials/
  index.json        Manifest (generated by tools/update_manifest.py)
  metals/           alsi10mg.json, aluminum-1100-o.json, aluminum-2024-t3.json,
                    aluminum-5052-h32.json, aluminum-6061-t6.json,
                    aluminum-6063-t5.json, aluminum-7075-t6.json,
                    beryllium-s65.json,
                    brass-free-cutting-astm-b16-h02-temper.json,
                    copper-c11000.json,
                    inconel-625.json, inconel-718.json,
                    invar-36-alloy.json, magnesium-az31b-h24.json,
                    maraging-300-dmls-aged.json,
                    nitinol-shape-memory.json, nitinol-superelastic.json,
                    stainless-17-4ph-h1025.json, stainless-17-7ph-th1050.json,
                    stainless-303.json, stainless-304.json, stainless-316.json,
                    stainless-316l-dmls.json,
                    steel-1018-cold-drawn.json,
                    steel-4130-normalized.json,
                    steel-4340.json, steel-a2.json, steel-h13.json,
                    steel-mild-a36.json, steel-spring-5160.json,
                    titanium-6al-4v.json, titanium-ti-6al-4v-grade-5-sta.json,
                    titanium-ti64-dmls-heat-treated.json
  plastics/         abs.json, acrylic-pmma.json, pa6-nylon6.json,
                    pa12-nylon12.json, peek.json, petg-fdm.json,
                    pha-fdm.json, pla-fdm.json, polycarbonate.json,
                    pom-delrin.json, uhmw-pe.json
  ceramics/         alumina-al2o3.json, silicon-carbide.json,
                    tungsten-carbide-wc-6co.json
  composites/       c-sic-woven.json, cfrp-ud.json, gfrp-woven.json,
                    kevlar49-epoxy-ud.json, synfoam-sw9-syntactic-foam.json
  elastomers/       epdm.json, fluorocarbon-viton.json, nitrile-nbr.json,
                    silicone-vmq.json
  glass/            bk7-optical-glass.json, borosilicate-glass.json,
                    ule-glass.json, zerodur-grade-0.json
  natural/          balsa-wood.json, birch-plywood.json

references/index.json   Shared reference database (BibTeX cite keys)
schema/v1.json          JSON Schema Draft-07

tools/
  validate.py         Validate all material JSONs against schema
  update_manifest.py  Regenerate materials/index.json
  migrate.py          Schema migration tool (dry-run default; --write to apply)
  import_bibtex.py    BibTeX import tool (dry-run default; --write to merge)
  import_new_refs.py  Promote new_references from material JSONs → references/index.json (dry-run default; --write to apply)
  download_refs.py    Fetch each URL in references/index.json and save as <stub>.html/.pdf
                        ⚠ Save to the OneDrive folder below — HTML/PDF files are gitignored
                        --delay SECS   pause between requests (default 1.5)
                        --force        re-download files that already exist
  parse_refs.py       Parse downloaded HTML files into material-schema-format JSON (canonical units)
                        ⚠ Always pass the OneDrive path as <html_dir> — never a path inside the repo
                        <html_dir>     directory of downloaded HTML files
                        --glob PATTERN filter files, e.g. "azom*.html" (default: *.html)
                        --output DIR   output directory (default: <html_dir>/parsed/)
  compare_refs.py     Compare material JSON values against parsed reference data
                        <pattern>      filename glob, e.g. aluminum*.json or *.json
                        --refs-dir DIR directory of parsed reference JSONs (required)
                        --tolerance N  relative tolerance, e.g. 0.03 = 3% (default: 0.05)
                        --no-fix       report only; do not prompt to update files
                        --verbose      also list properties that match their references

Reference HTML/PDF storage (outside the repo, gitignored):
  ~/Library/CloudStorage/OneDrive-UniversityofVictoria/Documents/Work/Research/Materials Database/
  Typical workflow:
    python tools/download_refs.py "$REFS_DIR"
    python tools/parse_refs.py "$REFS_DIR"
    python tools/compare_refs.py "*.json" --refs-dir "$REFS_DIR/parsed"
  where REFS_DIR is the OneDrive path above.

  Fetching new reference pages (established 2026-07):
    - MatWeb: never fetch directly — its terms of service prohibit automated scraping.
      Always ask for a manually-downloaded HTML file and a file path.
    - Other sites (manufacturer datasheets, AZoM, Wikipedia, academic papers via PMC, etc.):
      fetch directly with plain `curl` — no spoofed/browser-impersonating User-Agent, that's
      a bot-detection-evasion technique that's off the table even for sites without a known
      anti-scraping policy. If a plain request gets blocked (403, Cloudflare challenge, etc.),
      treat that as the site declining automated access and fall back to asking for a manual
      download — don't route around the block. Manufacturers' own sites (e.g. eos.info,
      synfoam.com) are frequently more complete and less restrictive than third-party
      distributor/reseller mirrors (e.g. Knowde) for the same datasheet.
    - After a successful fetch, save the raw HTML/PDF into the OneDrive folder above using
      the same `<stub>.html`/`.pdf` naming convention as a manual download, so it's a
      permanent citable snapshot usable by `parse_refs.py`/`compare_refs.py` later.

tests/
  units.test.js        Node built-in test runner (node:test) — conversion factors in js/core/units.js
  derived.test.js       Same — merit indices and shear/stiffness formulas in js/core/derived.js
  Run: `node --test tests/` (or `npm test`). Requires Node — not part of the
  site's own runtime (still no build step, no bundler, no dependencies), only
  a dev-time check. package.json exists solely to set "type": "module" so
  Node parses js/core/*.js as ES modules; it has no dependencies and no
  build/serve script.

.github/
  workflows/validate-schema.yml   CI: runs validate.py (+ new_references check) on materials/schema/references changes
  workflows/js-tests.yml          CI: runs `node --test tests/` on js/core/ or tests/ changes
  PULL_REQUEST_TEMPLATE.md        Checklist for material submissions
```

## Canonical Storage Units

All values stored in canonical units in JSON. Convert only at display time.

| Property | Canonical Unit | Notes |
|---|---|---|
| Pressure / Moduli | GPa | |
| Compressive Strength | MPa | Exception — stored in MPa not GPa |
| Fracture Toughness | MPa·m^0.5 | Display as MPa·m½ using `<sup>½</sup>` |
| Density | g/cm³ | Display both g/cm³ and kg/m³ simultaneously |
| Temperature | °C | `melting_point_tm` stores the **solidus** temperature; document the liquidus in the material's `notes` field |
| CTE | µm/m·K | displayed as µm/m·K (°C and K are identical in magnitude) |
| Thermal conductivity | W/m·K | |
| Specific heat | J/kg·K | |
| Thermal diffusivity | cm²/s | |
| Electrical conductivity | % IACS | |
| Hardness | Vickers (HV) | |

## Key Conventions

### Material JSON
- `valued_property` pattern: `{"value": number|null, "ref": "bibtex-key"|null}`
- All ref keys must exist in `references/index.json`
- Reference entries have: `short_label`, `doi`, `bibtex`, optionally `url`

### innerHTML escaping
Every page that interpolates material-derived strings (`name`, `category`, etc.) or URL-derived values (query params) into `innerHTML` must run them through a local `escHtml()` helper first (duplicated per-file: `browse.js`, `detail.js`, `compare.js`, `select.js`). Only `name` is schema-unconstrained free text (`slug`, `category`, `usage_frequency` are pattern/enum-constrained) — but escape all of them for consistency and to survive future schema changes.

### Detail page (`js/pages/detail.js`)
- Unit conversion: `data-canonical` + `data-canonical-unit` attributes; number set via `textContent`, unit appended as text or as a `<span>` with `innerHTML` only for entries in the hardcoded `UNIT_HTML` map (MPa·m½, ksi·in½)
- Temperature and electrical conductivity selectors are document-wide scope
- Reference badges: `_refNums` Map built once at start of `render()` → `[N]` anchor links to `#ref-N`
- Reference link priority: DOI → `entry.url` → BibTeX `url` field → ISBN WorldCat → none
- BibTeX title regex: `/title\s*=\s*\{+([^{}]+)\}+/` (handles single and double braces)
- Global unit presets: Metric = GPa/MPa/MPa·m^0.5/K/% IACS; Imperial = ksi/ksi/ksi·in^0.5/°F/% IACS
- Magnetic classification renders as colored badge: `.badge-ferro`, `.badge-para`, `.badge-dia`

### Submit page (`js/pages/submit.js`)
- Per-field reference dropdowns populated from `references/index.json`
- Add-reference form has two tabs: BibTeX/Manual and URL
- Edit button on each ref panel item opens form pre-populated; key field locked during edit
- On download: converts display units → canonical before writing JSON; sets `schema_version: 1` and `submitted_date`
- **Edit mode:** `?slug=<slug>` in URL triggers edit mode — fetches existing material, pre-fills all fields, locks slug, changes button to "Download Updated JSON", shows replace-file instructions after download
- `thermal_conductivity` and `specific_heat` use `type: 'thermal-table'` field type — same UI pattern as CTE (single value + temperature-dependent table with add/remove rows). Getters: `getThermalTable(fieldId, valueKey, canonicalUnit)`. Prefill: `prefillThermalTable(fieldId, valueKey, prop)`.
- New references added via the form are embedded in the downloaded JSON under `new_references`; after merging the PR, run `python tools/import_new_refs.py --write` to promote them into `references/index.json`, then delete the `new_references` block from the material file (the tool doesn't strip it, and CI fails on main if it lingers)
- **Pre-fill from HTML:** upload a saved reference page (AZoM, MakeItFrom, MatWeb, SpaceMatDB, TheWorldMaterial, EngineersEdge, HighTempMetals, eFunda, NIST — same sites as `tools/parse_refs.py`; unrecognized filenames fall back to a generic best-effort table scraper) via `js/core/htmlImport.js`'s `parseHtmlToMaterial()`. If the file has a `<link rel="canonical">`/`og:url` tag, a reference is auto-created and linked to every field the parser populated; otherwise the UI prompts for the URL (or lets you skip creating a reference). **Overwrite behavior is deliberate, not a bug:** like "Pre-fill from JSON," it unconditionally overwrites any field it has data for — including fields you already typed in or that came from a prior import — and reassigns that field's reference dropdown too. Re-importing the same file, or importing over a field with a different curated reference, will silently clobber it; there is no merge/undo. This makes multi-source import (e.g. AZoM for mechanical properties, then NIST for thermal) the intended workflow, but re-review the form after each import.
  - **⚠ Duplication warning:** `js/core/htmlImport.js` re-implements the same 10 site-specific scraping rules as `tools/parse_refs.py`, independently, in JavaScript. The two are *not* kept in sync automatically. If a site changes its page layout, both files need fixing — check both when one breaks. This was an accepted tradeoff (see PR history), not an oversight: running Python in-browser (Pyodide) would add a ~10-20MB WASM dependency against this project's plain-JS/no-build-step stack, and a shared declarative config wasn't fully viable since a couple of parsers (HighTempMetals, MatWeb) have real procedural logic, not pure selector+regex extraction.
  - **Row-aggregation is first-match-wins per schema path in both tools.** AZoM rows like "Hardness, Rockwell B/C (converted from Brinell hardness...)" spuriously substring-match the `hardness_brinell` pattern; last-write-wins let that silently overwrite a correct primary-row value with an unrelated one (found twice — `azom-4340.html` during development, then again on `azom-1018.html` while sourcing a real material, since the fix hadn't been mirrored into `tools/parse_refs.py` yet at that point). Both `htmlImport.js`'s `applyRows()` and `tools/parse_refs.py`'s `_apply_rows()` now use first-match-wins to fix this — don't revert either back to last-write-wins.

### Shear strength derivation
When no sourced shear strength value is available **and the material is ductile** (metals that yield via dislocation slip), derive using von Mises criterion: `τ = UTS / √3`. Store the computed value with `"ref": null` and note the derivation method in the material's `notes` field. Do not use the 0.6×UTS empirical approximation.
**Do not apply von Mises to brittle materials** (ceramics, cemented carbides/cermets, most fibre composites) — they fail by brittle fracture, not plastic yielding, so the criterion doesn't physically apply. Leave `shear_strength` null for these and say why in `notes` (see `tungsten-carbide-wc-6co.json`, `kevlar49-epoxy-ud.json`) rather than compute a physically meaningless number.

### Fatigue S-N curve
`mechanical_other.fatigue_sn_curve` stores `points` (max stress in GPa vs. cycles — **not** stress amplitude, except when `stress_ratio` = -1, where they're numerically equal), plus `stress_ratio` (R = σ_min/σ_max) and `test_method` (`"Axial"`, `"Rotating Beam"`, `"Plane Bending"`, `"Torsion"`, or `null`).
- **Every point in one curve must share the same R and test method.** Never combine data from different stress ratios, loading types, or notched/unnotched conditions into one curve — if a source gives multiple conditions, either pick one consistent condition or (if genuinely useful) treat it as a separate entry, not extra points on the same curve.
- Leave `stress_ratio`/`test_method` `null` if the source doesn't document them — **do not guess.** An undocumented curve is still useful data, but `compare.js` and `detail.js` will flag it as "conditions undocumented" so students know not to treat it as directly comparable to other materials.
- When a choice of R is available, prefer **R = -1 (fully reversed), Axial** for new entries — this is what `aluminum-2024-t3.json` and `aluminum-7075-t6.json` use (both verified), keeping fatigue comparisons on the compare page meaningful across materials rather than incidentally apples-to-oranges. `aluminum-6061-t6.json` is also R=-1 but with `test_method: null` (its source's underlying data mixes axial and rotating-beam tests, so no single method could be honestly assigned) — that's the right call when a source is explicit about R but not about method, rather than guessing.
- Document the derivation in `notes` the way `aluminum-2024-t3.json`/`aluminum-7075-t6.json`/`aluminum-6061-t6.json` do: cite the source equation/figure, sample size, R² if given, and flag any point that extrapolates beyond the source's directly-tested life range. If a source publishes both a raw best-fit curve and a safety-factored design/code-allowable curve (see `aluminum-6061-t6.json`'s notes for the Yahr 1993 example), use the raw best-fit — a design allowable has an intentional knock-down factor baked in and would misrepresent the material's actual behavior.
- Do **not** add notch-geometry fields (Kt, root radius) to this schema — notched-specimen fatigue data (e.g. NASA TN D-111) is out of scope for this database; store only unnotched curves here.
- Comparing curves with different R/test method isn't fixed by labeling alone — `compare.js` shows a caveat banner (`#sn-caveat`) whenever the selected materials' curves don't share one fully-documented (R, test method) basis. Don't remove this without an equivalent safeguard; normalizing curves across different R values (e.g. a Walker equivalent-stress transform) was considered and rejected as a default behavior since it needs a per-material fitted exponent most sources don't provide — silently estimating one would fabricate precision this database doesn't actually have.

### Adding a new property
1. Add to `schema/v1.json`
2. Add to seed material JSON files
3. Add tooltip to `js/core/tooltips.js`
4. Add row to `renderPhysical()` / `renderMechanicalCommon()` etc. in `detail.js`
5. Add field to `FORM_SECTIONS` in `submit.js` and handle in `buildMaterialJSON()` / `prefillForm()`

## Magnetic Classification
Three values: `"Ferromagnetic"` (incl. ferrimagnetic — strongly magnetic, caution near field-sensitive instruments), `"Paramagnetic"` (weakly magnetic, generally acceptable), `"Diamagnetic"` (magnetically benign, preferred for precision optical/astronomical instruments).

## Export Feature
`js/core/export.js` provides `buildRows(materials, unitSystem)`, `downloadCSV()`, and `downloadXLSX()`. Both detail and compare pages have a "Download ▾" dropdown with four options: CSV/Excel × current-units/canonical. SheetJS loaded via CDN in `material.html` and `compare.html`. Sections exported: IDENTIFICATION, MECHANICAL, PHYSICAL, CTE VS TEMPERATURE (if table data present), FATIGUE S-N CURVE (if points present), MERIT INDICES.

## What's Next
1. Reference punch list — remaining materials with ref gaps: **Alumina Al₂O₃** (specific_heat, thermal_diffusivity, melting_point_tm)
1b. **Fatigue curve audit (2026-08-20 — partially resolved same day):** `stress_ratio`/`test_method` were added to the schema. Of the 6 materials with real fatigue data:
    - `aluminum-2024-t3`: **done.** R=-1, Axial, verified against MIL-HDBK-5J's own equivalent-stress equation for unnotched sheet (Figure 3.2.3.1.8(e)).
    - `aluminum-7075-t6`: **done.** The originally-stored curve (310/230/165/152 MPa) didn't reproduce either MIL-HDBK-5J unnotched equation (rod/bar 3.7.6.1.8(a), sheet 3.7.6.1.8(d)) at any consistent R — replaced with a verified curve computed from the sheet equation at R=-1, Axial (246/165/111/57 MPa), same product form/methodology as 2024-T3.
    - `aluminum-6061-t6`: **done**, but from a different source than originally cited. MIL-HDBK-5J's only unnotched equation for this alloy (rod/bar, Figure 3.6.2.2.8) didn't reproduce the originally-stored curve at any consistent R either. Replaced with Yahr (1993, ORNL/ASME `yahr1993-6061t6-fatigue`) — a best-fit curve compiled from 5 literature sources, explicitly fully-reversed (R=-1); test_method is `null` because the paper itself states the underlying data mixes axial strain-controlled (low-cycle) and rotating-beam (some high-cycle) tests, so no single method can be honestly assigned. **Use the paper's raw best-fit equation (page 5), not its Table 1 design-curve values** — those have a nuclear-code safety factor (2x stress or 20x life) baked in and would misrepresent the material's actual fatigue behavior as far worse than reality.
    - `aluminum-5052-h32`: **still open.** MIL-HDBK-5J has no unnotched fatigue chart for this alloy at all. The only lead found so far is a bare single point (115 MPa endurance limit, H32 temper) from an Austral Wright Metals / Aluminium Development Council of Australia datasheet, with no cycle count, R, or test method stated — not solid enough to use. ASM Handbook Vol. 19 (*Fatigue and Fracture*) and the *Fatigue Data Book: Light Structural Alloys* were identified as likely real sources but are print/paywalled; worth checking if either is accessible.
    - `aluminum-1100-o`: still open — source is a 1984 print handbook (ASM Metals Handbook Desk Edition) with no available digital text to check against.
    - `brass-free-cutting-astm-b16-h02-temper`: resolved as genuinely undocumented (not a citation problem) — MatWeb's own page states no R/test method for its one fatigue point, confirmed via `tools/parse_refs.py`'s parsed output.
1c. **Fatigue data coverage is thin (found 2026-08-20, steel pass started same day):** originally only 6 of 62 materials had any actual S-N data points. `steel-4130-normalized` has since been added (raw NACA TN 2324 axial data, R=-1, 6 points including a run-out — no research needed, the source was already in hand from the 1b work). 7 materials still don't have the `fatigue_sn_curve` key at all (`gfrp-woven`, `stainless-303`, `stainless-316`, `steel-4340`, `steel-mild-a36`, `petg-fdm`, `pla-fdm`); the rest have the key present with an empty `points: []` placeholder.
    - **Recurring blocker for `steel-mild-a36`, `steel-1018-cold-drawn`, `stainless-304`, `stainless-316`:** extensive web searching for each kept dead-ending at the same source — **ASM Handbook Vol. 1** (*Properties and Selection: Irons, Steels and High-Performance Alloys*) and/or **Vol. 19** (*Fatigue and Fracture*) — which is print/paywalled and inaccessible from here. Freely available alternatives found were all unusable: bare single endurance-limit points with no R/cycles/method (5052-H32-style), notched-specimen data (out of scope), or strain-based LCF data (ANL/Chopra 2002 for 304/316 — real, rigorous, 500 tests, explicitly fully-reversed axial, but only converts to *one* legitimate stress-based point near yield; below that the pseudo-stress = strain×E relationship exceeds the material's actual UTS, i.e. not real stress). **Before repeating this search from scratch, check whether ASM Handbook Vol. 1 or Vol. 19 is accessible (UVIC library, etc.)** — a single scanned page could likely unlock all four at once, the way the user's manual download of the Yahr paper unlocked 6061-T6.
    - `stainless-303` and `steel-4340` remain deprioritized (Specialty-usage, not in the original priority list).
    - Remaining un-investigated targets from the original priority list: **aluminum-6063-t5, copper-c11000** (metals), **abs, pa6-nylon6, polycarbonate, pom-delrin, uhmw-pe, petg-fdm, pla-fdm** (plastics), **cfrp-ud, gfrp-woven** (composites) — worth trying before another attempt at the ASM-Handbook-blocked steels, since generic/common plastics and composites may have better-documented free sources (e.g. manufacturer datasheets, MatWeb).
    - When sourcing new curves, follow the R=-1/Axial-by-default guidance in the Fatigue S-N curve conventions section above, and don't repeat the original 1b mistake — verify the stored numbers actually reproduce the cited source's own equation/chart before trusting the citation. Watch for design/allowable curves (like Yahr 1993's Table 1) that apply a safety-factor knock-down — use the raw best-fit/test data, not a code-allowable curve, or the stored value will misrepresent the material.
2. CI / merge-process gaps:
   - Manifest (`materials/index.json`) is regenerated manually after merge — consider a CI step that runs `update_manifest.py` and fails if `git diff` is non-empty (cheaper than a bot-commit approach, same guarantee)
   - ~~No CI check that all `ref` keys exist in `references/index.json`~~ — **this was wrong** (corrected 2026-08-20): `validate.py` checks both the top-level `references` array and every property-level `ref` against `references/index.json`, and CI runs it on every PR. Keys listed in a file's own `new_references` are exempted (correctly — that's the submit-form workflow).
   - The *actual* gap: nothing detects a `new_references` block lingering on `main` after merge (the exemption above means validate.py passes forever). Found in practice: `titanium-ti-6al-4v-grade-5-sta.json` sat unpromoted for a month+. CI now fails if any material on main carries `new_references` (see validate-schema.yml, push-only step); the fix when it fires is `python tools/import_new_refs.py --write` **followed by deleting the `new_references` block from the material file** — the tool only adds to the index, it does not strip the block (brass carried an already-promoted block for 3 months for exactly this reason).
3. ~~Additional seed materials — 316L SS (AM), Maraging Steel 300, Ti-6Al-4V (AM)~~ — **done** (2026-07): added `stainless-316l-dmls`, `maraging-300-dmls-aged`, `titanium-ti64-dmls-heat-treated`, plus a larger batch covering the full original recommendation list (`steel-1018-cold-drawn`, `aluminum-6063-t5`, `pa6-nylon6`/`pa12-nylon12`, `polycarbonate`, `stainless-17-4ph-h1025`, `aluminum-2024-t3`, `stainless-17-7ph-th1050`, `magnesium-az31b-h24`, `tungsten-carbide-wc-6co`, `nitinol-shape-memory`/`nitinol-superelastic`, `kevlar49-epoxy-ud`, `synfoam-sw9-syntactic-foam`, `titanium-ti-6al-4v-grade-5-sta`).
4. Candidates noted during the above but not acted on — pick up if useful:
   - **Markforged 3D-printed 17-4 PH / Kevlar composite** — real MatWeb entries seen while sourcing 17-7 PH and Kevlar; would round out the AM/print-specific cluster
   - **Wrought Maraging 300** (hot-forged/rolled bar or sheet, cast) — the current `maraging-300-dmls-aged` entry is AM-specific only; a wrought comparison would enable `fabrication_processes` entries (hot forging, rolling, cold forming, casting) that don't apply to a printed part
   - **CMT HYTAC machinable tooling-board syntactic foam** — a distinct use case from the buoyancy-focused `synfoam-sw9-syntactic-foam` entry (model-board/tooling stock vs. underwater buoyancy)
   - Denser Synfoam grades (SW-12/15/18) or other depth-rated lines (MW/DW/HP) if a deeper-water comparison point is wanted alongside the shallow-water SW-9 entry
   - **Ti-6Al-4V AM as-built** condition specifically — not currently represented (EOS's own datasheet doesn't report it and recommends against using it unheat-treated); worth adding only if a source with genuine as-built mechanical data turns up
5. **Repo review follow-ups (2026-08-20).** A full review pass found and fixed several defects same-day (S-N axis mislabel on detail page, doc counts 61→62, stale punch-list claim about CI ref-checking, lingering `new_references` + new CI check for it, stray `Beam_Design_Stiffness.xlsx`, `validate.py` relative-path crash, `parse_refs.py` cp1252 console crash — commits `df795fa`/`fe248fb`). Remaining items, in recommended order:
   - ~~**JS tests for `units.js` and `derived.js`**~~ — **done and verified** (2026-08-20): `tests/units.test.js` and `tests/derived.test.js` (Node's built-in `node:test`, no dependencies), CI at `.github/workflows/js-tests.yml`. First CI run failed 1/64 (`convertElectrical(100, 'MS/m')` asserted `=== 58` via strict equality, but `100 * 0.58 = 57.99999999999999` in IEEE 754 — a test bug, not a units.js bug); fixed with a tolerance-based `approx()` helper. Confirmed 64/64 passing with a real local Node install (portable zip, `winget`'s MSI needs UAC elevation this environment can't grant non-interactively) — not hand-traced, actually executed.
   - **Physical-plausibility linter** — schema validates shape, not physics; the 2026-08-20 fatigue audit proved values can be wrong-but-schema-valid for months. A warnings-only pass (in `validate.py` or a sibling `tools/lint_physics.py`): solidus < liquidus (notes vs. `melting_point_tm`), ductility 0–100%, yield ≤ UTS, Poisson 0–0.5, S-N stress monotonically decreasing with cycles, density sane per category, `usable_temp_range.min < max`. Warnings, not CI failures — some legitimate materials will trip naive rules.
   - **Manifest-freshness CI step** — run `tools/update_manifest.py` in CI and fail if `git diff --exit-code materials/index.json` shows drift (cheaper than bot-commits, same guarantee; supersedes the older "auto-regenerate and commit" idea in item 2).
   - **`loader.js` cache-policy inconsistency** — manifest and references fetch with `cache: 'no-store'` but material JSONs use default HTTP caching (`loader.js:40`), so a returning visitor can mix a fresh manifest with stale material files. Pick one policy (probably `no-store` everywhere, or drop it everywhere and trust GH Pages' 10-min max-age).
   - Smaller/optional, from the same review: generate the README/SPEC material tables + counts from `materials/index.json` instead of hand-syncing four docs (they drifted twice already); split the 1,954-line `submit.js` into ES modules (no-build constraint forbids a bundler, not multiple files); `aria-label`s on chart canvases (Chart.js canvases are opaque to screen readers; CSV export partially mitigates); tidy `derived.js` (redundant alpha double-read at `derived.js:26-28`, truthiness `E && rho` vs `!= null` inconsistency in merit `fn`s — benign today, a trap if copied for a property that can legitimately be 0).
   - ~~Loose thread: confirm the new lingering-`new_references` CI step's first run was green~~ — **confirmed** (2026-08-20): GitHub Actions run for `fe248fb` (head commit at that push, includes `df795fa`'s check) completed with conclusion `success`.
